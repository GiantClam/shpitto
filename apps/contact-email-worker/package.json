{
  "name": "contact-email-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preflight": "node --experimental-strip-types scripts/preflight.mts",
    "sync:secrets": "node --experimental-strip-types scripts/sync-secrets.mts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260405.0",
    "@types/node": "^20",
    "typescript": "^5.9.3",
    "wrangler": "^4.15.2"
  }
}
